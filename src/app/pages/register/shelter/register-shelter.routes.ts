import { Routes } from '@angular/router';
import { RegisterShelterComponent } from './register-shelter.component';

export const SHELTER_ROUTES: Routes = [
  { path: '', component: RegisterShelterComponent },
];
