import { Routes } from '@angular/router';
import { RegisterPersonComponent } from './register-person.component';

export const PERSON_ROUTES: Routes = [
  { path: '', component: RegisterPersonComponent },
];
