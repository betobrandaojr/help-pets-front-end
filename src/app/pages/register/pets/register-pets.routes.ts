import { Routes } from '@angular/router';
import { RegisterPetsComponent } from './register-pets.component';

export const PETS_ROUTES: Routes = [
  { path: '', component: RegisterPetsComponent },
];
